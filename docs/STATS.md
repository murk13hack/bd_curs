# Статистика — словарь метрик

На графиках подписаны оси (единицы: шт., %, баллы 1–5, минуты). Под каждым графиком — краткое пояснение в `ChartCaption`. Недельный обзор: задачи (шт.), паттерны (%) и настроение (1–5) на **разных** осях Y.

## Период 7 / 30 / 90 д

Влияет на: KPI, обзор (недели, heatmap, время), задачи, дневник, OLAP.

**Не влияет** на вкладку «Паттерны» в таблице — там всегда скользящие **30 д** по расписанию (`v_pattern_streaks`).

## KPI «Паттерны (свод)»

`patterns_success / patterns_scheduled` — сумма по всем паттернам и дням за период (день×паттерн), не среднее `%` по карточкам.

## OLAP

Источник: `v_olap_daily_facts` (grain: пользователь × день).

Меры в процентах: `completion_rate`, `pattern_clean_rate`.

## Дневник

`GET /stats/holistic` — недели из OLAP, не только дни с дневником. Корреляции Пирсона требуют несколько дней с вариацией внутри недели.

`GET /stats/correlation` — классический срез «дневник + задачи по дедлайну».
